#!/bin/bash

dir=$1

test -n $dir || dir=.

echo $(realpath $dir)
echo ------------------------------------------------

git -C $dir log --pretty=oneline --decorate -1
echo ------------------------------------------------
git -C $dir status
